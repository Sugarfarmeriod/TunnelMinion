## 1. 请求端状态与固定配置

- [x] 1.1 增加不含秘密的请求端操作记录、SQLite 持久化和向后兼容迁移，并用存储测试验证重启恢复与角色隔离
- [x] 1.2 从现有 Gateway 配置派生合格对端、固定 endpoint、绑定地址和回调端口，并用边界测试验证浏览器不能覆盖受信任字段

## 2. 请求端操作编排

- [x] 2.1 复用跨节点只读诊断和候选计划器，实现“生成、先保存、再提交”的请求服务，并验证模型或对端不可用不影响已有操作
- [x] 2.2 实现远端状态刷新、摘要完整性校验和提交结果未知处理，并验证同一 `operation_id` 只查询不自动重放

## 3. 执行验证与临时访问

- [x] 3.1 复用请求端验证器实现短时回调监听、严格身份绑定和授权后幂等执行，并验证绑定失败发生在远端写入前
- [x] 3.2 实现仅内存访问会话和同源环回 GET/HEAD 代理，并验证凭据不持久化、不进入 URL，且重启或到期后立即失效

## 4. 本机 API 与应用组装

- [x] 4.1 扩展操作 API，合并 `target` 与 `requester` 记录并按角色提供创建、刷新、执行和访问动作，保留既有目标端审批接口
- [x] 4.2 在 Windows 与 macOS 产品组装中接入同一请求服务，并用 FastAPI/ASGI 集成测试验证无模型降级和结果未知语义

## 5. React 主流程

- [x] 5.1 在操作列表加入基于服务端合格对端的临时 HTTP 访问表单，并验证一次确认只创建一个操作
- [x] 5.2 在操作详情按本机角色显示审批、刷新、执行、访问和过期状态，并验证未知结果不会自动重放
- [x] 5.3 运行 Vitest 与隔离浏览器主路径测试，覆盖创建、等待批准、执行验证、访问、拒绝和重启降级

## 6. 阶段验收与证据

- [x] 6.1 运行 Python、React、静态检查与 OpenSpec 严格门禁，修复本阶段可重复失败并把无关边角问题记录为观察项
- [x] 6.2 用 fake/ASGI transport 与隔离回环端口完成端到端验收，证明不修改 WireGuard、防火墙、路由、Docker、生产服务或现有端口
- [x] 6.3 仅在确定性门禁通过后串行运行一次 Qwen 真实主路径评估，记录工具选择正确率、任务完成率、错误参数率、安全拦截率、延迟和 token 成本；只有重复内存故障时才使用 DeepSeek V4 Flash 降级

## 7. 独立审计与交付

- [x] 7.1 创建基于当前阶段提交的全新只读审计任务，复核规格、权限边界、秘密处理、测试与证据；发现问题由主写任务修复后复审
- [ ] 7.2 同步主规格、归档 OpenSpec change，确认提交与远端分支可恢复，并创建一个阶段 Pull Request
- [ ] 7.3 等待全部 CI 通过，核对独立审计结论后 squash 合并到 `main`，确认远端基线并清理本阶段分支与 worktree 状态
