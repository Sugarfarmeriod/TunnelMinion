## 1. 冻结基线

- [x] 1.1 新增只读基线清单，绑定 `101377a`、数据集/Prompt 哈希、模型、工具版本、预算、scorer、运行次数、报告路径及六项分子分母
- [x] 1.2 增加基线加载与比较条件校验，证明条件不一致时不会生成能力提升结论

## 2. 建立 Skill 与状态契约

- [x] 2.1 定义最小版本化 Investigation Skill 契约并注册 `service.local-only@1`，覆盖语义证据、执行位置、允许工具和终态条件
- [x] 2.2 为 incident 增加向后兼容的 `InvestigationState`，持久化阶段、预算、步骤、facts、unknowns、Skill 版本和停止原因
- [x] 2.3 验证旧 incident JSON 往返、秘密拒绝和中断恢复，成功步骤在显式恢复后不重复执行

## 3. 跑通 local_only Harness

- [x] 3.1 将 `local_only` 接入 Skill 路径，保留其他事件现有行为，并让程序核验语义证据覆盖与冲突
- [x] 3.2 在同一调查中路由目标 Gateway 取证和请求端可信参数探测，禁止本机监听或进程结果冒充远端证据
- [x] 3.3 覆盖正常、超时、离线、无权限和取消终态，展示 facts、unknowns、证据、工具位置、预算、Skill 版本和 stop reason

## 4. 建立同口径评测与演示

- [x] 4.1 扩展场景结果和 scorer，保存每项分子/分母及证据覆盖、重复成功步骤和过早停止指标
- [x] 4.2 更新固定远端 `local_only` 场景，验证四类语义证据、缺证据 unknown、冲突拒绝和请求端独立探测
- [x] 4.3 提供隔离可重复演示命令和人可读报告，不读取秘密、不调用写工具、不修改真实网络
- [x] 4.4 校验前后比较条件；旧基线与新报告的 dataset、模型和 scorer 不同，因此明确记录不可比且不写提升数字

## 5. 质量门禁与交付

- [x] 5.1 运行 incident、Gateway、评测定向测试和 OpenSpec strict 校验
- [x] 5.2 运行 Python 全量格式、类型、测试与覆盖率门禁，核对 diff 不含秘密、运行时数据或页面分支改动
- [x] 5.3 整理可追溯证据并提交推送阶段成果；按用户要求跳过独立只读审计任务
