## 1. 依赖、基线与单一写入边界

- [x] 1.1 现场记录 `origin/main`、PR #40、PR #59、相关 OpenSpec 完成度、CI、review 和 worktree，建立依赖矩阵；Draft PR 证据只标记为 `draft-pr-verified`
- [x] 1.2 为本 change 指定唯一主写者和公共文件写入顺序，确认不修改已归档 LPE/managed-path artifacts
- [x] 1.3 在写 README、前端或最终演示材料前，确认所需 PR 已进入明确稳定基线；未满足时只保留本 change 规划并记录 blocker
- [x] 1.4 定义 claim-to-evidence manifest schema，覆盖状态分类、来源 SHA/PR、平台、环境、验证方式、采集时间、适用范围和脱敏资产

## 2. 主叙事与信息架构

- [x] 2.1 固定 30 秒产品定位和中性 PDF 服务场景，证明设备/服务入口、访问地址、只读诊断、授权、验证和恢复闭环
- [x] 2.2 编写三分钟逐秒脚本，使用 `Thread → Run → Operation → Evidence` 串联产品画面，并标明每一步的现场动作、预期状态和兜底资产
- [x] 2.3 定义五至十分钟技术深挖入口，分别覆盖 Agent/确定性工具边界、目标节点授权、幂等与恢复、跨平台降级和证据追踪
- [x] 2.4 明确 A/B/C 三 Agent 直接协作、邀请入网、n2n/relay、模型 fallback/额度路由和 `Mission` 均为后续独立能力，不进入首版成功声明

## 3. 展示素材与产品承载

- [x] 3.1 在稳定基线上设计 README 展示结构：一句话定位、闭环动图、五步主流程、证据表、生命周期图、安全边界、降级矩阵和深挖链接
- [x] 3.2 复核 PR #40 合并后的 Overview、Chat、Operations、Memories、Settings 是否足以承载完整故事；只有存在经证据确认的缺口时才规划最小只读聚合视图
- [x] 3.3 生成现场演示、录屏和离线证据的素材清单，记录每项资产的 source SHA、采集环境、图源/导出关系和脱敏检查
- [x] 3.4 按单一写入者和另行授权门禁处理 Penpot；仓库 Mermaid/SVG/manifest 必须离线可验证，主演示不以架构课件开场

## 4. 失败、降级与离线评估

- [x] 4.1 建立明确标记的离线 fixture，覆盖模型故障、节点离线、超时、重复/乱序事件、矛盾证据、候选未授权、执行失败和恢复失败
- [x] 4.2 验证无模型时设备/服务状态、访问地址和已有操作控制继续可用，且录屏播放不会被描述为当前现场成功
- [ ] 4.3 建立评估集并测量工具选择正确率、任务完成率、错误参数率、安全拦截率、端到端延迟、模型调用次数、token 与成本；先跑真实基线再确定阈值
- [ ] 4.4 对所有 fixture、历史证据和 Draft PR 证据运行声明分类检查，确保它们不计入稳定 `main` 或本轮真实 A/B 成果数字

## 5. 稳定基线上的真实素材采集

- [ ] 5.1 仅在用户对精确 A/B 资源、影响范围和停止/恢复方式明确授权后，引用或执行相称的真实网络与操作门禁；未授权时保持本任务未完成
- [ ] 5.2 在 Windows/macOS 稳定基线上重新执行主演示，保存完整公开事件、授权、验证、恢复/清理、提交、平台和采集时间 provenance
- [ ] 5.3 录制与相同稳定提交对应的主流程和一个故障降级场景，验证画面不含秘密、隐藏推理、未脱敏工具数据或 `planned` 成功占位
- [ ] 5.4 生成最终截图、离线 SVG、claim-to-evidence manifest 和量化证据表，并核对所有数字来自本轮命令、报告或真实环境记录

## 6. 发布门禁与交付

- [ ] 6.1 运行受影响 Python/前端测试、跨平台浏览器与 package 门禁、文档链接、Mermaid/SVG 安全检查、秘密扫描和 `openspec validate prepare-interview-showcase --strict`
- [ ] 6.2 由独立只读审计核对主叙事、授权边界、失败降级、证据来源、素材与稳定提交一致性；发现问题后交回唯一主写者修复并复审
- [ ] 6.3 检查完整 diff、工作树、生成物和任务勾选范围，以独立 Conventional Commit 提交并普通 push，创建面向 `main` 的 Draft PR
- [ ] 6.4 CI 与独立复审通过后裁决展示 change；合并后同步 `interview-showcase` 主规格并归档 change
