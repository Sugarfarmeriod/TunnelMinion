## 1. 契约与前端工具链验证

- [ ] 1.1 固定四张旧页面、现有 API/OpenAPI、CSP、Windows/macOS package、静态资源体积和关键用户流程基线，建立不读取秘密的前端 fixture
- [ ] 1.2 用最小 spike 验证 React + TypeScript + Vite 在开发服务器、FastAPI 同源路径和无 Node.js 的离线 package 中加载，并证明刷新子路由不会 404
- [ ] 1.3 以包体积、可访问性、维护成本和许可证为依据选择轻量组件基础与浏览器测试范围，记录接受/否决结论；不得把未经验证的 UI 库直接作为既定依赖
- [ ] 1.4 固定本地、peer、模型、Coordinator、operation、memory 的视图模型与新鲜度/稳定错误码矩阵；如现有 API 不足，只提取最小脱敏聚合读模型
- [ ] 1.5 运行 spike、依赖许可证/漏洞、生成物秘密、OpenSpec strict 和现有 Python 回归门禁，提交并推送工具链阶段

## 2. React 外壳与安全静态交付

- [ ] 2.1 建立带锁文件的前端目录、统一格式/类型/单元/组件/生产构建命令和 CI 缓存边界，生产运行时不得依赖 Node.js
- [ ] 2.2 实现同源 API client、运行时响应校验、查询缓存、新鲜度传播和统一错误映射，不在浏览器持久化秘密或完整诊断 payload
- [ ] 2.3 实现统一外壳、路由、导航、设计 token、主题基础、加载/空/陈旧/失败组件和可恢复 Error Boundary
- [ ] 2.4 由 FastAPI 提供 SPA 入口与内容哈希静态资源，应用严格同源 CSP、入口 no-store、哈希资源缓存和子路由 fallback；保留旧页面回退路径
- [ ] 2.5 覆盖远端 HTML/script 文本、外部资源、路径穿越、缓存误报实时、键盘焦点和窄窗口矩阵，运行前后端门禁并提交推送外壳阶段

## 3. 节点与服务总览纵向切片

- [ ] 3.1 实现本机摘要、已知节点、服务和关键依赖卡片，显示证据来源、最后时间、新鲜度和可执行下一步，不以原始 JSON 作为主界面
- [ ] 3.2 分别表达 local running、peer accepted、模型状态、Coordinator 实时/陈旧/离线/未配置、managed/static 路径和 local-only
- [ ] 3.3 实现节点与服务详情、非敏感筛选/深链接及诊断细节入口，未知或不兼容状态不得被折叠成正常
- [ ] 3.4 覆盖无模型、无 Coordinator、peer 离线、防火墙日志不可读、缓存过期和刷新恢复组件/浏览器测试
- [ ] 3.5 在 Windows/macOS 开发运行中执行总览人工验收，记录状态解释正确率、错误动作率、首次加载和刷新延迟、压缩资源体积，提交并推送总览阶段

## 4. 聊天与公开证据纵向切片

- [ ] 4.1 实现 thread 新建、选择、继续、删除和消息展示，删除确认必须明确不影响独立长期记忆
- [ ] 4.2 实现 run 发起、取消和公开工具轨迹，只展示目标节点、工具、状态、耗时、tool run ID 与证据引用，不展示隐藏推理或无限原始输出
- [ ] 4.3 实现以事件序号为边界的 SSE 断线重连、去重、完成关闭、页面恢复和 stale run 处理，不自动重放工具或写操作
- [ ] 4.4 覆盖模型不可用、工具失败、取消竞态、重复事件、事件缺口、恶意正文、超长内容和组件卸载连接清理
- [ ] 4.5 运行固定 AI 评估集，确认工具选择正确率、任务完成率、错误参数率、安全拦截率、延迟和成本不低于既有门槛，并提交推送聊天阶段

## 5. 操作审批纵向切片

- [ ] 5.1 实现待批准、执行中、已验证、回滚/撤销、过期、未知结果和 `cleanup_failed` 的列表与详情状态
- [ ] 5.2 实现批准、拒绝、取消和撤销的对象明确确认、单次提交与服务端状态复读；超时后只查询相同 operation，不盲目重试写请求
- [ ] 5.3 展示目标证据、风险、访问者、端口、有效期、授权依据、验证、回滚、执行资源和脱敏错误，浏览器可见性不得替代服务端权限
- [ ] 5.4 覆盖重复点击、过期批准、权限拒绝、网络超时、请求节点离线、回滚失败、模型/Coordinator 离线时撤销和恢复矩阵
- [ ] 5.5 在隔离资源上执行 Windows/macOS 真实操作 UI 验收，证明批准对象正确率、安全拦截率、恢复成功率和零秘密输出，提交并推送审批阶段

## 6. 记忆、设置与诊断纵向切片

- [ ] 6.1 实现长期记忆的来源、用户/网络/节点作用域、更新时间、确认、修正、单条删除和精确作用域清空，并明确其不是聊天历史
- [ ] 6.2 实现模型、Coordinator、runtime、版本和可选诊断来源的脱敏设置/状态页；秘密只通过现有受限配置流程处理且不得回显
- [ ] 6.3 实现诊断导出入口与用户可理解的恢复步骤，确保没有 Murus、日志权限或厂商 VPN CLI 时仍可使用产品核心功能
- [ ] 6.4 覆盖跨作用域隔离、删除竞态、错误修正、秘密字段、诊断包脱敏、无模型清理和键盘/屏幕阅读状态提示
- [ ] 6.5 运行前后端全量门禁与 Windows/macOS 人工验收，提交并推送记忆/设置阶段

## 7. Package、迁移与产品验收

- [ ] 7.1 将锁定 React 生产构建接入统一 package 暂存流程和清单摘要，不提交手工生成文件；构建失败不得复用陈旧 dist
- [ ] 7.2 构建 Windows amd64 与 macOS arm64 版本化 package，在无 Node.js、无源码、无网络和独立 profile/data/secret 环境验证界面、API、SSE 与旧页面回退
- [ ] 7.3 执行桌面与规定窄窗口的键盘、焦点、语义、非颜色状态、加载性能、CSP、不可信文本和浏览器存储安全验收
- [ ] 7.4 在真实 A/B 上验证总览、聊天、peer 状态、操作控制、模型/Coordinator/peer 故障降级和恢复；不修改客户防火墙、WireGuard、路由、模型、自启动或生产秘密
- [ ] 7.5 更新用户与开发文档，能用 Mermaid 流程图表达的启动、降级、审批和回退流程优先使用流程图，并核对所有本地链接
- [ ] 7.6 核对并按需更新主 FigJam 的界面边界、同源 API、控制/数据面和当前/历史标记；外部额度不足时记录 blocker，但不得绕过阶段关闭门禁
- [ ] 7.7 运行 React 与 Python 全量质量、依赖/许可证/秘密、安全、双平台 package、真实 A/B、文档链接、架构图和 OpenSpec strict 门禁，提交推送并创建 PR
- [ ] 7.8 合并后同步 `local-product-interface` 主规格、复核发布分支构建与回退，再归档 change
