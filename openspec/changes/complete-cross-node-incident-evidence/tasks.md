## 1. 远端授权与准备

- [ ] 1.1 为 static peer 只读解析、允许工具交集、缺失凭据和目标不匹配编写定向测试
- [ ] 1.2 复用 `FixedGatewayClient` 与 `RemoteCapabilityLoader` 实现共享远端 incident 工具准备器，并通过定向测试

## 2. Incident Agent 跨节点闭环

- [ ] 2.1 为 Windows caller → macOS execution、摘要预检证据、远端证据路径和调用关联编写调查测试
- [ ] 2.2 接入 `local_node_id` 与远端准备器，修正 caller/execution，保证远端纠正后不 fallback 且失败诚实停止
- [ ] 2.3 在 Windows/macOS 本地应用工厂接入同一准备器，验证无配置、模型失败和本机 incident 回归不受影响

## 3. 固定评测与平台证据

- [ ] 3.1 升级固定 incident 数据集和报告，加入远端来源/平台、真实隔离 Gateway、预检分类、远端完成率与本机误执行硬门槛
- [ ] 3.2 增加 Windows/macOS 隔离跨节点验收回执及矩阵校验，证明同一提交、固定场景、零秘密读取和零系统写入
- [ ] 3.3 增加最终指标冻结校验，绑定实现提交、数据集/Prompt/工具哈希、模型身份、平台回执、真机回执和一次最终模型报告

## 4. 当前 A/B 与最终模型门禁

- [ ] 4.1 运行格式、类型、定向/全量测试、覆盖率、安全扫描和 OpenSpec strict 门禁，提交并推送可复现候选
- [ ] 4.2 使用自有临时端口、目录和进程完成 Windows A → macOS B 只读验收，核对生产 8080/8787 与网络前后不变并清理自有资源
- [ ] 4.3 在同一候选和固定数据集上只运行一次 Qwen 最终评测；仅在重复 OOM 时按授权改用 DeepSeek 并如实记录
- [ ] 4.4 生成并校验最终指标冻结清单，安全硬门槛或质量目标不满足时不宣称阶段完成

## 5. 独立审计、合并与清理

- [ ] 5.1 创建全新干净的用户可见只读审计任务，核对规格、实现、证据、平台边界和无生产修改；修复问题后复审
- [ ] 5.2 同步稳定规格并归档 change，运行最终门禁后创建本阶段唯一 PR
- [ ] 5.3 等待 CI 全绿，复核 PR 与审计证据，squash 合并到 `main` 并只清理本阶段分支/worktree
