## 1. 确定性交接与待办规则

- [x] 1.1 实现并单测 incident 候选处理显示规则，覆盖已确认远端 `local_only`、本机目标、陈旧/消失服务、未知端口和未确认状态
- [x] 1.2 实现并单测 Operation 待办投影，只包含本机待批准、待执行、写结果未知和清理失败记录

## 2. Overview 与操作表单主路径

- [x] 2.1 在 Overview 增加独立失败的待处理操作卡片和现有详情链接，不在摘要内提供写动作
- [x] 2.2 在 incident 详情增加受限候选处理入口与不可处理说明，点击仅导航且不发起模型或写请求
- [x] 2.3 在操作页校验并展示 incident 预填上下文，预填合格目标和端口；目标失效时不得静默替换或提交

## 3. 组合产品验收

- [x] 3.1 完成 React 组件与 API 回归测试，证明预填仍需明确确认、创建只发送一次且 Operation API 失败不影响 incident 总览
- [x] 3.2 增加隔离 Playwright 路径，走通远端 incident → 预填表单 → Operation 详情，并验证 Overview 待办筛选和零自动 POST
- [x] 3.3 更新正式包浏览器验收，确认默认 Overview 可发现既有目标端待审批记录，不修改真实网络或服务

## 4. 阶段门禁与模型成本证据

- [x] 4.1 运行与本阶段相称的 Python、React、Playwright、静态检查和 OpenSpec 严格门禁，修复可重复回归
- [x] 4.2 确定性门禁通过后串行运行一次现有 Qwen 候选计划主路径评测，记录工具选择、完成率、错误参数、安全拦截、延迟和 token；仅重复 OOM 时降级 DeepSeek V4 Flash

## 5. 独立审计与交付

- [x] 5.1 提交并推送阶段实现，创建全新用户可见只读审计任务复核规格、权限边界、上下文信任和测试证据；问题由主写任务修复后复审
- [ ] 5.2 同步稳定主规格、归档唯一 OpenSpec change，创建一个阶段 Pull Request 并等待全部 CI 通过
- [ ] 5.3 核对独立审计和 CI 后 squash 合并到 `main`，确认远端基线并清理本阶段分支与 worktree 状态
