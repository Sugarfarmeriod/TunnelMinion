## 1. 状态快照与 Incident 合同

- [x] 1.1 定义有界规范化快照、差异事件、incident、候选假设、公开轨迹和报告合同，并验证秘密/业务正文不能进入合同
- [x] 1.2 基于现有本机资源与 Coordinator 服务目录组装快照，保留来源、新鲜度、观测时间和修订
- [x] 1.3 实现固定六类差异、确认窗口与稳定去重键，验证正常刷新和短暂抖动不创建 incident
- [x] 1.4 增加 incident 持久化、状态迁移和重启中断恢复，禁止自动重放模型或远端工具
- [x] 1.5 运行快照/差异/存储定向门禁，复核正常刷新零模型调用后提交并推送本阶段

## 2. 事件驱动调度与单 Agent 调查

- [x] 2.1 接入有界后台观察调度、incident 触发和单 incident 单 run 并发约束
- [x] 2.2 复用 Context Runtime 实现候选假设—工具选择—证据更新循环，并只暴露现有六个只读工具
- [x] 2.3 强制模型轮次、工具调用、墙钟与上下文预算，以及充分证据、信息不足、预算耗尽、取消和失败停止条件
- [x] 2.4 生成区分事实、候选解释、未知项、停止原因和证据引用的公开报告，拒绝无证据确认结论
- [x] 2.5 验证模型不可用、未知工具、参数错误、工具失败与 Runtime 重启降级后提交并推送本阶段

## 3. Overview Incident 体验

- [ ] 3.1 扩展现有 Overview 只读契约，提供有界 incident 列表与详情，不建立第二套聚合后端
- [ ] 3.2 在 React Overview 增加 incident 卡片、状态与证据时间，并在详情展示公开轨迹、结论、未知项和停止原因
- [ ] 3.3 复用现有 conversation/Context Runtime 增加绑定 incident 的小型追问，确保不修改原证据或扩大工具权限
- [ ] 3.4 根据未知项和证据缺口生成有界建议问题，并在模型增强不可用时使用固定模板
- [ ] 3.5 运行 API、组件、键盘/窄窗口和不可信文本定向门禁后提交并推送本阶段

## 4. 固定故障矩阵与价值门禁

- [ ] 4.1 建立覆盖服务新增/消失、节点离线、目录陈旧、环回监听、远端不可达、Docker 不可用、工具失败、模型失败和预算耗尽的固定离线矩阵
- [ ] 4.2 计算根因成功率、工具选择率、非必要调用率、无证据断言率、失败恢复率和端到端延迟，并按版本保留失败分类
- [ ] 4.3 增加正常刷新零模型调用、单 incident 单 Agent、禁止工具零执行和无证据确认零容忍门禁
- [ ] 4.4 在不修改防火墙、WireGuard、路由或服务的前提下完成本机端到端验收；真实 A/B 只读验收另行取得明确授权
- [ ] 4.5 运行与本 change 相称的完整门禁、OpenSpec strict、diff 与秘密复核，记录结果后提交并推送最终阶段
