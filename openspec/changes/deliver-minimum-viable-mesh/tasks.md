## 1. 技术验证与工程基线

- [ ] 1.1 记录现有 A（Windows）与 B（macOS）的 WireGuard 拓扑、地址、接口、配置位置和启停方式，生成不含私钥的测试环境说明与恢复检查表
- [ ] 1.2 建立 Python 3.11+ 项目基线，验证依赖管理、类型检查、异步测试、Windows/macOS/Linux 构建和候选可执行文件打包方式，并用 ADR 固化选择
- [ ] 1.3 验证候选控制通道的版本协商、双向状态同步、断线重连和本地调试体验，并用 ADR 固化传输与序列化方案
- [ ] 1.4 建立 Python 单体仓库目录、统一命令入口、格式化、静态类型检查、单元测试和 Windows/macOS/Linux CI
- [ ] 1.5 定义秘密信息分类和日志脱敏测试，确保 token、设备私钥和认证头不会进入普通日志

## 2. 共享协议与可测试内核

- [ ] 2.1 定义节点身份、虚拟地址、peer、连接路径、服务记录、快照修订号和错误码的版本化协议模型
- [ ] 2.2 实现控制消息的兼容性校验、幂等快照应用和不兼容版本错误测试
- [ ] 2.3 定义 `NetworkProvider`、平台安全存储、监听端口枚举器、Docker 元数据源和服务探测器接口
- [ ] 2.4 实现内存协调服务、模拟 Agent 和模拟网络 Provider，完成两个节点注册、配置下发和状态收敛的无网卡端到端测试
- [ ] 2.5 建立结构化诊断模型，覆盖注册状态、控制通道、网络配置、peer 路径、最近错误和扫描时间

## 3. 协调服务与节点注册纵向切片

- [ ] 3.1 实现协调服务配置加载、TLS 启动、健康检查和受控关闭
- [ ] 3.2 实现 enrollment token 的生成、过期、单次/限次使用和撤销，并增加失败场景测试
- [ ] 3.3 实现节点公钥注册、稳定节点 ID、虚拟地址租约和重复注册幂等处理
- [ ] 3.4 实现节点心跳、在线/离线状态、协议版本拒绝和节点撤销
- [ ] 3.5 实现协调服务持久化边界和首个嵌入式存储适配，验证重启后身份与地址租约保持稳定
- [ ] 3.6 实现 Agent 设备密钥生成、平台受保护存储适配和凭据日志脱敏
- [ ] 3.7 完成 Agent 使用 token 首次注册、重启恢复身份、断线重连和撤销后的端到端测试

## 4. WireGuard 连通纵向切片

- [ ] 4.1 实现 Python `NetworkProvider` 的只读观察模式，在不修改配置的前提下读取 A 的现有 `HomeMac` 隧道和 B 的现有 WireGuard 状态
- [ ] 4.2 实现网络配置校验、原子应用、保留最近有效配置和失败清理测试
- [ ] 4.3 实现受管资源所有权记录，并用独立名称、接口和配置目录创建首个 TunnelMinion 测试隧道，验证不会覆盖用户现有配置
- [ ] 4.4 实现 peer 候选端点交换、直连探测和 `connecting/direct/offline` 状态报告
- [ ] 4.5 实现协调节点 WireGuard 路由回退，并显示 `relayed` 状态及协调节点信任边界提示
- [ ] 4.6 实现回退路径与恢复直连之间的自动切换，验证活动连接的可接受中断行为
- [ ] 4.7 实现控制面短时离线时保留最近有效隧道和后台重连逻辑
- [ ] 4.8 在现有 A/B 环境完成只读状态展示，并在独立测试隧道完成直连、强制回退、重新直连、协调服务重启和 Agent 完整清理验收

## 5. 三端网络与系统服务适配

- [ ] 5.1 实现并测试 Windows WireGuard Provider，兼容官方客户端和 tunnel service，确认任何清理操作都不会影响现有 `HomeMac` 接口
- [ ] 5.2 实现并测试 macOS WireGuard Provider，兼容 B 当前手写配置与终端启停流程，并将用户配置与 TunnelMinion 受管配置隔离
- [ ] 5.3 实现并测试 Linux WireGuard Provider、权限检查、systemd 服务启动和卸载清理
- [ ] 5.4 为权限不足、依赖缺失、地址冲突和残留接口提供平台一致的用户错误与修复建议
- [ ] 5.5 在 CI 或受控测试机上完成 Windows/macOS/Linux 两两组合的注册、重连和基础连通矩阵

## 6. 自动服务发现纵向切片

- [ ] 6.1 实现服务快照、稳定服务 ID、来源、置信度、可访问性和新鲜度模型
- [ ] 6.2 实现 Windows、macOS、Linux 的 TCP/UDP 监听端口枚举和尽力而为的进程关联，并测试权限降级
- [ ] 6.3 实现可选的只读 Docker 元数据源，关联容器名称、镜像、标签和已发布端口，确保不读取环境变量或执行控制操作
- [ ] 6.4 实现带超时、并发和响应大小预算的 HTTP/HTTPS 基础探测器及未知服务回退
- [ ] 6.5 实现 `local-only` 与虚拟网络可访问性判断，确保环回服务不会生成远端打开链接
- [ ] 6.6 实现 Agent 完整快照同步、协调服务按节点存储、服务停止处理和离线 TTL
- [ ] 6.7 在不配置任何大模型 API 的环境完成端口、Docker、未知服务、扫描失败和目录过期端到端验收

## 7. 本地面板纵向切片

- [ ] 7.1 实现仅绑定 `127.0.0.1` 和 `::1` 的本地 Agent API，并增加非环回地址不可访问测试
- [ ] 7.2 实现节点列表和详情视图，展示当前节点、在线状态、虚拟地址、连接路径和诊断摘要
- [ ] 7.3 实现按节点组织的服务目录，安全渲染远端文本并展示类型、协议、地址、端口、可访问性和新鲜度
- [ ] 7.4 为可访问 HTTP/HTTPS 服务实现打开操作，为其他服务实现规范化 `host:port` 复制操作
- [ ] 7.5 为 `local-only`、离线和过期服务禁用误导性操作并展示原因
- [ ] 7.6 实现控制面离线、网络配置失败、peer 不可达、扫描降级和服务过期的独立状态展示
- [ ] 7.7 将前端静态资源嵌入 Agent，并在 Windows、macOS、Linux 浏览器环境完成一致核心体验验收

## 8. 安全、恢复与发布验收

- [ ] 8.1 为注册、节点撤销、配置下发、本地 API 和远端文本渲染建立威胁模型与安全回归测试
- [ ] 8.2 验证 Agent 和协调服务崩溃、升级前停止、网络切换及异常断电后不会破坏用户已有网络配置
- [ ] 8.3 完成三节点场景验收：A、B、C 均显示本机和远端节点，至少一条直连、一条回退路径及对应服务目录
- [ ] 8.4 完成性能基线，记录空闲资源、扫描开销、目录同步延迟、直连吞吐和回退吞吐
- [ ] 8.5 编写开发者启动、协调服务部署、Agent 加入、故障诊断和完整卸载文档
- [ ] 8.6 生成预览构建和校验和，在三端干净环境执行安装、加入、使用、退出和清理的发布检查表
