## 1. 可复现合同

- [x] 1.1 覆盖六类本机事件的相关证据路径、逐轮信息缺口、可达性工具依赖和 `state_stale` 无模型保守终态
- [x] 1.2 覆盖模型无工具或过早终态时只纠正一次、再次违例才走受控 fallback，且远端/目录来源不纠正、不借用本机工具
- [x] 1.3 覆盖必要工具失败、结构化不可用、可信参数不完全相等、一次可重试无效模型响应和工具或墙钟预算耗尽由 Runtime 直接裁决，并保留已有公开轨迹与工具证据
- [x] 1.4 覆盖确认结论必须引用完整证据路径、遗漏引用只修复一次、五类实时事件的确定性冲突不得确认、远端症状证据不得冒充根因以及候选假设保持公开
- [x] 1.5 覆盖模型请求不含场景 ID、评分必要工具、期望根因或夹具参数，并锁定新版 Prompt 的版本和关键约束

## 2. 最小实现

- [x] 2.1 在现有 `IncidentInvestigator` 内维护事件证据路径、已尝试/成功工具、当前可用工具和公开信息缺口，不新增 Planner 或持久化类型
- [x] 2.2 在现有模型循环加入一次有界工具合同纠正、确定性只读 fallback、工具依赖、完全相等的可信参数、失败终止和预算前置检查
- [x] 2.3 收紧最终确认覆盖、因果充分性、生产输出可用性、快照一致性与引用门槛，复用现有 trace 持久化纠正、证据与剩余缺口，不由 Runtime 生成根因
- [x] 2.4 版本化事故调查 Prompt，并新增符合生产工具输出合同、评分只认已证实公开内容且拒绝反向状态词的固定矩阵数据集，保持历史数据集与报告不可变
- [x] 2.5 用只读 `docker ps -a` 覆盖退出容器，不增加 Docker 控制能力或读取额外敏感字段

## 3. 质量与真实验收

- [x] 3.1 运行调查、Prompt、评测和 Web 定向回归，再运行完整测试、Ruff、Pyright 与 OpenSpec strict 校验
- [x] 3.2 在不修改模型服务的前提下核对前后健康状态，用精确 Qwen 本地路径和同一固定场景生成可追溯正式报告
- [x] 3.3 确认无依据断言、正常刷新模型调用、禁止工具执行和冲突确认均为零；根因、工具选择、失败恢复与任务完成率至少 0.8，不必要工具调用率不高于 0.25，并记录延迟、token、fallback 与错误参数
- [ ] 3.4 创建独立干净的用户可见只读审计任务，复核规格、实现、测试、报告来源和门禁；修复后复审至无阻断项
- [ ] 3.5 同步主规格、归档本 change，提交并推送阶段恢复点，通过单一 Pull Request 的全部 CI 后 squash 合并到 `main` 并清理本阶段分支
