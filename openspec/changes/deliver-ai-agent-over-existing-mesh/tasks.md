## 1. 工程、协议与安全基线

- [ ] 1.1 建立 Python 3.11+ 单体仓库、依赖锁定、统一命令、格式化、类型检查、单元测试和 Windows/macOS CI
- [ ] 1.2 定义 node ID、thread ID、run ID、tool run ID、工具 schema、错误码和版本兼容模型
- [ ] 1.3 建立秘密、实时状态、短期上下文、checkpoint、长期记忆和审计数据的分类与保留规则
- [ ] 1.4 编写 MVP 威胁模型，覆盖模型提示注入、工具参数注入、远端未认证、秘密泄露、跨 namespace 访问和拒绝服务
- [ ] 1.5 用最小原型比较 MCP remote transport 与版本化 HTTP/RPC 网关，并以 ADR 记录认证、取消、能力发现和可观察性选择
- [ ] 1.6 定义假模型、假工具和固定 Agent 评估场景格式，完成不依赖真实 API 的 CI 冒烟测试

## 2. 模型 Provider 与本地配置纵向切片

- [ ] 2.1 实现模型 Provider 接口、工具调用能力声明、结构化输出、超时、取消和使用量元数据
- [ ] 2.2 实现首个用户可用的兼容模型 Provider，并用真实 API 验证工具调用与结构化结果
- [ ] 2.3 实现 Windows 与 macOS 本机密钥存储适配，确保 Web API、日志、异常和导出内容不返回完整密钥
- [ ] 2.4 实现模型配置 CRUD 和连通/认证/模型/能力验证 API，区分可诊断错误
- [ ] 2.5 实现模型不可用降级：资源 API 与工具继续运行，新 AI run 返回明确状态
- [ ] 2.6 为有效配置、无效密钥、超时、不兼容模型、删除配置和秘密脱敏增加自动测试

## 3. 结构化 Tool Runtime 纵向切片

- [ ] 3.1 实现工具注册表和风险策略，包含名称、版本、schema、平台、权限、超时、结果预算和数据敏感级别
- [ ] 3.2 实现参数校验、未知工具拒绝、超时、取消、并发限制、输出截断和结构化错误
- [ ] 3.3 实现关联 thread/run/tool run ID 的脱敏审计记录，并测试失败调用同样可追踪
- [ ] 3.4 实现假工具适配器，覆盖成功、参数错误、超时、大结果、提示注入文本和平台不支持
- [ ] 3.5 建立只读策略测试，证明 `requires-approval`、`forbidden` 和任意动态工具不会进入 MVP 模型工具集合
- [ ] 3.6 建立平台状态快照测试护栏，证明完整只读工具序列不会改变 WireGuard、路由、容器或服务状态

## 4. Windows A 节点只读工具与资源视图

- [ ] 4.1 实现 Windows `get_wireguard_status`，读取 `HomeMac` 状态但过滤私钥和敏感配置
- [ ] 4.2 实现 Windows `list_network_listeners` 与 `get_process_summary`，支持权限不足时结构化降级
- [ ] 4.3 实现 Windows `list_docker_services`，只读关联容器、镜像和端口，不读取环境变量或执行控制操作
- [ ] 4.4 实现带地址/端口校验、超时和读取预算的 `probe_service_reachability`
- [ ] 4.5 实现 `get_node_summary`，聚合平台、Agent、模型、WireGuard 和工具能力状态
- [ ] 4.6 建立 A 节点资源 API 与基础资源页面，在无模型配置时展示真实只读状态
- [ ] 4.7 在调用前后比较 `HomeMac` 服务、接口、路由和配置摘要，完成人工只读验收

## 5. 本地对话 Agent 纵向切片

- [ ] 5.1 使用 LangChain 实现首个只读工具调用 Agent，并注入动态工具集合而不是全量固定工具
- [ ] 5.2 实现最大模型轮次、最大工具次数、墙钟超时、成本/使用量记录和用户取消
- [ ] 5.3 实现本地 thread/run API 和流式事件，展示目标、工具状态、耗时与证据引用，不展示隐藏推理
- [ ] 5.4 实现基于 `tool_run_id` 的证据化回答格式，区分事实、推测、未知和停止原因
- [ ] 5.5 实现工具结果不可信数据封装、长度限制和提示注入回归测试
- [ ] 5.6 实现本地聊天页面、线程列表、继续线程、新建线程、取消 run 和删除线程
- [ ] 5.7 使用假模型完成“查询 A 的 WireGuard/端口/Docker 状态”端到端测试
- [ ] 5.8 使用真实模型完成本地诊断验收，记录工具轨迹、参数、延迟、token 和成本基线

## 6. 上下文、checkpoint 与长期记忆

- [ ] 6.1 实现独立的 checkpoint、tool artifact 和 long-term memory 存储接口及 SQLite 适配
- [ ] 6.2 实现 Context Builder，对消息、工具 schema、工具结果和记忆分别应用硬预算
- [ ] 6.3 实现大型工具结果 artifact 化、相关片段选择、滚动摘要和实际上下文规模记录
- [ ] 6.4 实现 Runtime 重启后的线程读取和 interrupted run 标记，确认不会自动重放工具
- [ ] 6.5 实现带用户/网络/节点 namespace、来源和更新时间的长期记忆模型
- [ ] 6.6 实现仅保存用户确认事实/偏好的写入流程，拒绝秘密、实时快照和未确认模型推测
- [ ] 6.7 实现长期记忆查看、修正、单条删除和按作用域清空 API/UI
- [ ] 6.8 为长对话、大服务列表、旧状态冲突、会话隔离和记忆删除建立回归测试

## 7. macOS B 节点与远端 Tool Gateway

- [ ] 7.1 实现 macOS WireGuard 状态、监听端口、进程、Docker、可达性和节点摘要只读适配
- [ ] 7.2 验证 B 的手写 WireGuard 配置与终端启停流程在所有工具调用前后保持不变
- [ ] 7.3 按 ADR 实现 Tool Gateway、能力清单、工具版本协商和结构化远端错误
- [ ] 7.4 实现节点认证、只读允许列表、WireGuard 地址绑定、速率限制、超时、取消和响应大小限制
- [ ] 7.5 实现显式 A/B peer 配置和认证材料生命周期，不复用 WireGuard 私钥作为应用凭据
- [ ] 7.6 实现跨节点关联审计，使 A/B 可通过 run ID 与 tool run ID 对应同一次调用
- [ ] 7.7 使用固定客户端完成 A→B 能力发现、成功工具、非法参数、未认证、超时和离线测试
- [ ] 7.8 验证 B 未配置模型时仍可向 A 提供只读工具，而 B 本地资源页面正常降级

## 8. 跨节点 Agent 诊断闭环

- [ ] 8.1 实现节点摘要优先、目标确定后再加载远端能力的动态工具选择流程
- [ ] 8.2 实现远端端口、进程和 Docker 结果合并，生成带来源、时间、可访问性和置信度的服务摘要
- [ ] 8.3 实现从请求节点执行的服务可达性探测，并与远端监听范围、容器映射和 WireGuard 状态关联
- [ ] 8.4 完成“B 有哪些服务”场景，使 A 的 Agent 基于 B 的真实只读工具回答并提供证据
- [ ] 8.5 完成“B 的 PDF 服务为什么打不开”场景，识别环回监听并明确不执行修复
- [ ] 8.6 完成 B 离线、Docker 不可用、工具权限不足、远端超时和证据不足的失败场景
- [ ] 8.7 在 A/B 两端分别验证本地聊天、查询对端和单节点模型故障隔离
- [ ] 8.8 记录标志性场景的脱敏工具轨迹、截图和可重复人工验收步骤

## 9. Agent 评估与安全回归

- [ ] 9.1 建立包含正常、失败、提示注入、编造工具、非法参数、秘密请求和写操作请求的版本化离线数据集
- [ ] 9.2 实现期望工具命中率、非必要工具率、参数有效率、禁止工具调用数和平均工具次数指标
- [ ] 9.3 实现任务完成率、关键事实覆盖、证据冲突和未知信息标注指标
- [ ] 9.4 实现模型轮次、token、工具耗时、总延迟和可获得成本的采集与版本对比报告
- [ ] 9.5 在 CI 使用假模型/固定响应运行离线回归，并为安全失败设置零容忍门禁
- [ ] 9.6 使用选定真实模型运行固定评估，记录 prompt、模型、工具和数据集版本
- [ ] 9.7 在 A/B 真机运行远端服务发现、环回诊断、节点离线、Docker 不可用和模型失败评估
- [ ] 9.8 发布首份包含成功、失败案例和改进项的 Agent 评估报告，避免只展示最佳对话截图

## 10. 文档与 MVP 发布验收

- [ ] 10.1 编写架构图、工具风险模型、上下文/记忆边界和跨节点认证 ADR
- [ ] 10.2 编写 A/B 开发启动、模型配置、peer 配置、故障排查、数据导出和完整卸载文档
- [ ] 10.3 在干净 Windows 与 macOS 环境验证安装、启动、模型降级、工具网关、停止和数据清理
- [ ] 10.4 执行安全检查，确认无 API key、WireGuard 私钥、认证材料或敏感工具正文进入仓库与报告
- [ ] 10.5 完成三分钟演示脚本和视频素材：发现 B 服务、诊断不可达、展示证据与安全边界
- [ ] 10.6 完成最终 OpenSpec 场景、自动测试、离线评估和 A/B 真机验收对照表
