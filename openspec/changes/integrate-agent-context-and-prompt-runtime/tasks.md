## 1. 生产调用盘点与契约基线

- [x] 1.1 盘点 Agent、诊断、评估与后台任务中的全部模型 Provider 调用点，记录当前 prompt、消息、工具 schema、历史、记忆和结果来源
- [x] 1.2 定义 `ContextRequest`、不可变 `ContextSnapshot`、内容引用、预算决定、裁剪原因和脱敏追踪字段
- [x] 1.3 建立架构测试，禁止生产代码绕过 `ContextBuilder` 直接构造 Provider 请求，并为假模型保留隔离的测试入口
- [x] 1.4 固定当前典型对话与跨节点诊断的回答、工具选择、延迟、token 和成本基线
- [x] 1.5 运行格式、静态类型、契约和基线评估门禁，提交并推送上下文契约阶段

## 2. 统一生产上下文入口

- [x] 2.1 扩展 `ContextBuilder` 以接受任务类型、当前意图、thread/run、动态工具集合、事实证据和分项预算
- [x] 2.2 让本地对话、跨节点诊断和其他生产模型调用只通过有效 `ContextSnapshot` 调用 Provider
- [x] 2.3 在快照中关联 prompt、模型/provider、工具 schema、证据快照和 Builder 版本，并校验缺失或不兼容版本
- [x] 2.4 覆盖直调拦截、无效快照、预算错误、Provider 失败与重复调用测试
- [x] 2.5 比较迁移前后典型任务的行为、延迟和成本，运行质量门禁并提交推送统一入口阶段

## 3. 对话历史、摘要与事实优先级

- [ ] 3.1 实现“当前消息 + 近期原文 + 未完成工作流状态 + 滚动摘要”的 thread 继续策略和独立历史预算
- [ ] 3.2 为摘要记录版本、覆盖范围、来源消息和失效条件，保证安全约束与工作流状态不依赖自由文本摘要
- [ ] 3.3 实现“实时工具证据 > 已确认适用记忆 > 历史/摘要 > 模型推断”的确定性冲突标记与组装顺序
- [ ] 3.4 覆盖长 thread、摘要失败、历史端口陈旧、服务状态变化和实时证据冲突测试
- [ ] 3.5 运行长对话任务完成率、证据一致性、裁剪正确性、延迟与成本门禁，提交并推送历史上下文阶段

## 4. 长期记忆隔离与生命周期

- [ ] 4.1 为记忆检索增加用户、节点、任务和安全命名空间硬过滤，再对通过过滤的已确认记忆做相关性排序
- [ ] 4.2 实现记忆确认、适用期限、修订链和删除 tombstone 的上下文可用性规则
- [ ] 4.3 让缓存、滚动摘要和候选集合按记忆 ID 响应删除或修订失效，不在后续上下文残留旧正文
- [ ] 4.4 覆盖错误记忆、未确认记忆、跨用户/节点/任务越权、删除残留和修订替代测试
- [ ] 4.5 运行记忆命中正确率、错误注入率、命名空间泄露零容忍和回归门禁，提交并推送记忆阶段

## 5. 工具结果制品化与上下文预算

- [ ] 5.1 定义分项预算和确定性裁剪顺序，防止历史、工具 schema、结果或记忆中的单一类别耗尽上下文
- [ ] 5.2 将超过单项预算的工具结果写入受控制品并生成脱敏预览、来源、大小、类型、截断标记和权限引用
- [ ] 5.3 实现制品后续读取的工具权限、预算和脱敏检查，禁止模型文本自动展开完整正文
- [ ] 5.4 覆盖大日志、二进制/未知类型、恶意正文、截断边界、制品缺失和越权引用测试
- [ ] 5.5 运行上下文上限、制品隔离、安全渲染和性能门禁，提交并推送制品阶段

## 6. Prompt 注册、版本与输入契约

- [ ] 6.1 建立代码库内 prompt 注册表，定义 `prompt_id`、语义版本、任务类型、模板、允许输入字段、内容哈希和中文变更说明
- [ ] 6.2 为现有生产 prompt 分配初始版本并改为从注册表渲染，远端文本、工具结果、历史与记忆保持不可信数据边界
- [ ] 6.3 在运行记录中关联 prompt 版本、内容哈希、模型参数、上下文快照和脱敏输入摘要
- [ ] 6.4 建立 prompt 变更版本检查以及黄金、回归和 prompt injection 对抗评估
- [ ] 6.5 运行任务正确性、证据引用、安全拦截、延迟、token 与成本比较门禁，提交并推送 prompt 生命周期阶段

## 7. 可观测性、归因与安全降级

- [ ] 7.1 记录各类消息、工具 schema、结果、制品、记忆的数量与大小，以及预算使用、摘要、裁剪和丢弃原因
- [ ] 7.2 实现 `context`、`prompt_or_model`、`harness_or_tool`、`governance` 主失败分类和可审计的脱敏次级原因
- [ ] 7.3 在 ContextBuilder、摘要或模型失败时停止本次 AI 推理，证明确定性工具 API、资源面板与已有安全操作控制仍可用
- [ ] 7.4 建立秘密、认证头、跨节点正文和完整凭据不进入普通日志、快照元数据或评估导出的零容忍测试
- [ ] 7.5 运行故障注入、安全、可观测性与降级门禁，提交并推送运行保障阶段

## 8. 综合评估、真机验收与文档

- [ ] 8.1 建立陈旧状态、长 thread、错误记忆、prompt injection、大结果、命名空间越权和删除记忆残留的离线评估集
- [ ] 8.2 在 Windows A/macOS B 完成“继续对话 → 动态选工具 → 远端诊断 → 实时证据覆盖旧信息 → 引用证据回答”的真机验收
- [ ] 8.3 统计工具选择正确率、任务完成率、错误参数率、安全拦截率、事实新鲜度、记忆隔离、prompt 版本覆盖率、延迟、token 和成本
- [ ] 8.4 更新架构、威胁模型、评估指南和标准概念映射，说明 Prompt、Context、Agent Runtime、Tool Runtime、Harness、治理和可观测性的职责
- [ ] 8.5 明确记录 Playbook/RAG、多 Agent、自动 prompt 优化、自修改 Harness、通用平台和企业 SaaS/RBAC 为后续或非目标
- [ ] 8.6 完成 OpenSpec 场景、自动测试、真机证据和任务对照，运行全部质量与安全门禁，提交并推送最终验收阶段
