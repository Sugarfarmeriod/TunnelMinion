## 1. 现状核对与代理技术验证

- [x] 1.1 记录当前 A/B Gateway、应用凭据、WireGuard 地址、本地 Web 端口和正式服务状态，生成不含秘密的操作前基线与恢复检查表
- [x] 1.2 选择一个隔离的本机 HTTP fixture 和 TunnelMinion 专用端口范围，确保验证不修改或占用用户现有服务
- [x] 1.3 使用最小原型比较 Python 内嵌 HTTP 代理与受管外部代理的绑定、流式、连接排空、超时、取消、打包和异常清理行为
- [x] 1.4 验证候选代理只能绑定显式 WireGuard 地址，能够转发到环回 HTTP 上游，并在进程崩溃后被独立恢复器识别和清理
- [x] 1.5 用 ADR 固化首个代理方案、支持的 HTTP 范围、临时凭据语义和未支持特性，并明确通用 TCP 转发仍为非目标
- [x] 1.6 运行格式、静态类型和既有测试门禁，提交并推送技术验证阶段

## 2. 操作协议、状态与持久化

- [x] 2.1 定义操作等级、计划、授权决策、预授权范围、租约、资源所有权、验证、回滚、审计和结构化错误模型
- [x] 2.2 定义 `planned`、`awaiting_authorization`、`authorized`、`executing`、`verifying`、`succeeded`、`expiring`、`expired`、`rolling_back`、`rolled_back` 和 `cleanup_failed` 状态转换规则
- [x] 2.3 为计划版本、幂等键、状态转换、绝对过期时间、跨节点 ID 和不兼容协议增加校验测试
- [x] 2.4 扩展 SQLite 边界以保存操作、授权、租约、资源指纹、验证和清理记录，并验证重启后状态保持稳定
- [x] 2.5 实现操作状态查询、列表和脱敏导出接口，确保完整凭据、认证头和远端不可信正文不会进入普通输出
- [x] 2.6 建立旧数据库无操作表时的可重复初始化和回滚测试，不改变现有 thread、memory、model 与 peer 数据
- [x] 2.7 运行格式、静态类型、协议与存储测试门禁，提交并推送状态基础阶段

## 3. L0～L4 策略与授权

- [x] 3.1 扩展工具注册与风险策略，使实际操作等级由确定性注册表决定且模型不能降低等级
- [x] 3.2 实现目标节点本地的逐次批准、拒绝、取消和授权过期决策，并记录操作者与决策依据
- [x] 3.3 实现同时约束请求 peer、工具、服务、端口、最长持续时间和有效期的 L2 预授权模型
- [x] 3.4 实现预授权创建、查看、撤销和匹配决策 API；聊天文本只能生成配置建议，不能直接创建授权
- [x] 3.5 覆盖无授权、完整命中、部分越界、授权过期、撤销后新请求、模型尝试提权和 L3/L4 请求拒绝测试
- [x] 3.6 建立安全门禁，证明服务重启、Docker 控制、WireGuard 修改、任意代码和秘密读取仍没有可执行工具路径
- [x] 3.7 运行格式、静态类型、策略和安全回归门禁，提交并推送授权阶段

## 4. 假适配器下的批准操作工作流

- [x] 4.1 实现结构化计划构建器，要求目标服务证据、预期变化、访问范围、风险、持续时间、验证和回滚字段完整
- [x] 4.2 实现执行前实时状态重读、服务指纹校验、计划版本校验和授权决策，拒绝过期或已经变化的计划
- [x] 4.3 实现持久化 Plan → Authorize → Execute → Verify → Rollback/Expire 工作流，确保模型不直接持有写适配器
- [x] 4.4 实现稳定幂等键和重复请求合并，覆盖请求超时后重试与跨节点重复提交
- [x] 4.5 使用假共享适配器与假请求节点完成逐次批准成功、预授权成功、拒绝、执行失败、验证失败和回滚失败测试
- [x] 4.6 实现模型不可用时的撤销、过期、恢复和清理路径，证明已有副作用不依赖模型才能终结
- [x] 4.7 记录操作阶段延迟、模型 token/成本、工具次数、授权方式和最终结果，为评估报告提供稳定字段
- [x] 4.8 运行格式、静态类型、工作流、恢复和覆盖率门禁，提交并推送假适配器纵向切片

## 5. HTTP 临时共享与资源所有权

- [x] 5.1 定义 HTTP 临时共享适配器接口和平台无关资源描述，拒绝通配、物理局域网、公网及非允许 WireGuard 地址
- [x] 5.2 实现首个代理适配器，将受控入口转发到已验证环回 HTTP 上游，并施加连接、请求、响应和墙钟预算
- [x] 5.3 实现租约内有效且可主动撤销的高熵临时凭据，确保日志、异常、模型上下文和诊断导出均脱敏
- [x] 5.4 实现入口端口冲突检查和自有资源指纹，任何创建、停止或删除操作只能作用于匹配的 TunnelMinion 资源
- [x] 5.5 实现过期连接排空、停止接受新请求和最终资源清理，记录正常到期与强制撤销的差异
- [x] 5.6 实现启动恢复器，处理已过期、未终结、资源缺失、资源指纹冲突和清理失败场景，且不自动重放写操作
- [x] 5.7 覆盖无效地址、错误凭据、未授权 peer、过期租约、端口占用、进程崩溃和未知资源不误删测试
- [x] 5.8 调用前后比较 WireGuard、路由、服务、Docker 和用户配置摘要，建立平台状态不变测试护栏
- [x] 5.9 运行格式、静态类型、适配器、安全与状态不变门禁，提交并推送 HTTP 共享阶段

## 6. Gateway 与请求节点验证

- [x] 6.1 扩展 Gateway 能力协议，区分只读工具、操作计划、授权状态、执行请求和验证结果，并拒绝不兼容版本
- [x] 6.2 在目标节点重新执行 peer 认证、操作允许列表、授权、计划版本、幂等、速率和响应预算检查
- [x] 6.3 实现请求节点验证器，使用本次临时凭据沿真实 WireGuard 路径检查状态码、响应预算和计划健康判据
- [x] 6.4 实现目标节点执行成功但请求节点超时、离线、认证失败或结果不符时的自动回滚协调
- [x] 6.5 关联 A/B 两端 operation、thread、run 与 tool run ID，使授权、执行、验证和回滚可以跨节点追踪
- [x] 6.6 覆盖未认证、错误 peer、计划篡改、重复执行、验证结果伪造、节点中途离线和远端清理失败测试
- [x] 6.7 运行格式、静态类型、Gateway、跨节点工作流和安全回归门禁，提交并推送远端验证阶段

## 7. 本地批准与操作生命周期界面

- [x] 7.1 为现有内嵌本地页面增加待批准计划视图，展示目标、证据、等级、风险、访问者、端口、有效时间、验证和回滚
- [x] 7.2 增加批准、拒绝、取消、主动撤销操作，并在提交前显示目标节点拥有最终授权权
- [x] 7.3 增加预授权查看、创建和撤销界面，要求用户分别确认 peer、工具、服务、端口、持续时间和授权有效期
- [x] 7.4 增加执行、验证、成功、回滚、过期和 `cleanup_failed` 状态视图，避免把“代理已启动”误显示为最终成功
- [x] 7.5 对远端服务名、计划说明、错误和审计文本进行安全渲染，不显示完整临时凭据或可复制认证头
- [x] 7.6 覆盖页面刷新、浏览器断线、Agent 重启、重复点击批准、过期计划和无模型降级交互测试
- [x] 7.7 运行格式、静态类型、Web/API、安全渲染和端到端 UI 门禁，提交并推送产品界面阶段

## 8. A/B 真机、安全与评估验收

- [ ] 8.1 在隔离 HTTP fixture 和专用端口上记录 A/B 操作前状态，确认没有复用正式服务凭据或用户配置
- [ ] 8.2 完成“Windows A 用户提问 → 只读诊断 → 结构化候选计划 → macOS B 逐次批准 → B 创建入口 → A 验证 → 浏览器访问 → 到期自动撤销”的标志性场景
- [ ] 8.3 完成同一场景的细粒度预授权执行，并验证越出任一授权范围时重新要求批准
- [ ] 8.4 注入错误凭据、端口占用、服务变化、A 离线、B 重启、验证失败、代理崩溃和清理失败场景
- [ ] 8.5 对每个场景比较 WireGuard、路由、Docker、原服务与用户配置摘要，证明只修改并清理 TunnelMinion 自有资源
- [ ] 8.6 扩展离线与真实模型评估集，统计计划字段与证据引用正确率、实时证据优先规则命中率、授权决策正确率、工具参数错误率、安全拦截率、任务完成率、prompt/上下文版本覆盖率、延迟、token 和成本
- [ ] 8.7 将禁止操作执行、安全边界绕过、凭据泄露、错误资源删除和验证失败却报告成功设为零容忍门禁
- [ ] 8.8 编写包含成功、拒绝、回滚、残留处理和已知限制的 A/B 真机评估报告及可重复人工验收步骤
- [ ] 8.9 在 Windows/macOS 干净环境验证安装、操作恢复、到期清理、停止和完整卸载，不遗留临时入口或凭据
- [ ] 8.10 将只读诊断、用户明确意图与结构化候选计划生成接入真实 Agent 路径，服务端重新校验必填字段、证据引用、计划版本和确定性操作等级
- [ ] 8.11 记录脱敏的 prompt、模型、provider、工具 schema、证据快照和上下文组成版本及大小指标，并验证上下文、模型、工具 Harness 与治理失败可分别归因
- [ ] 8.12 覆盖模型 Provider 和计划上下文组装失败的降级测试，证明只阻止新 AI 候选计划，已有批准、拒绝、撤销、过期、恢复和清理仍可使用
- [ ] 8.13 运行全部质量、安全、离线评估和发布验收门禁，提交并推送最终验收阶段

## 9. 文档与后续边界

- [ ] 9.1 更新架构、威胁模型和操作指南，使用“临时共享本机服务”术语，解释 L0～L4、批准、预授权、租约和所有权，并说明 Prompt 工程、Context 工程与 Harness 工程在本流程中的职责边界
- [ ] 9.2 更新三分钟演示脚本，展示诊断、计划、批准、执行、请求节点验证、临时访问和自动到期
- [ ] 9.3 记录 Playbook 候选数据结构，只保留适用条件、证据、动作、验证和回滚边界，不在本 change 实现检索或共享
- [ ] 9.4 明确记录通用 TCP、服务重启、Docker 控制、网络修改、Coordinator、多人权限和成熟独立前端属于后续 change；RAG、跨节点经验共享、多 Agent、自动 prompt 优化、自修改 Harness、通用 Harness 平台和企业 SaaS/RBAC 均不属于本 change
- [ ] 9.5 编写标准概念映射文档，把 TunnelMinion 当前组件对应到 Prompt 工程、Context 工程、Agent Runtime、Tool Runtime、Harness、治理、评估与可观测性，并明确不以 prompt 长度、Agent 数量、RAG/向量库存在与否或 Harness 层数衡量完成度
- [ ] 9.6 完成 OpenSpec 场景、自动测试、真机证据与任务的最终对照，准备独立 Pull Request 审阅
