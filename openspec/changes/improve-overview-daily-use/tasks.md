## 1. 规划与读取边界

- [ ] 1.1 核对三个 GET 的服务端读取链路，完成 proposal、design 和 delta spec，运行 OpenSpec 严格验证并提交推送。

## 2. 总览日常使用实现

- [ ] 2.1 服务每页 6 项，支持四类搜索、总数和范围、空结果与页码收敛；组件测试覆盖搜索、翻页、刷新保持和原证据。
- [ ] 2.2 三类当前查询每 30 秒只读刷新，顶部刷新覆盖全部当前读取，失败提示与追问草稿保持；定向测试证明不发写请求。
- [ ] 2.3 运行格式、类型、总览组件测试及构建门禁，核对变更范围后提交并立即推送。

## 3. 浏览器证据与独立验收

- [ ] 3.1 使用模拟的多服务、调查和待办验证正常、定时更新、失败恢复、搜索与输入保持、隐藏暂停及离开页面停止。
- [ ] 3.2 验证桌面与 320 CSS px 无横向溢出，键盘可达，保存浏览器截图并记录命令和结果；提交推送测试与证据。
- [ ] 3.3 协调者独立核对分支、提交、工作树、测试和截图后裁决验收，更新任务状态并处理 PR。

主写任务仅提供候选实现和证据，本清单保持未勾选，交由协调者独立核验。
