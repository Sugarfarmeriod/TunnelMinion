## 1. 可复现回归

- [x] 1.1 覆盖本机 `service_added` 首轮只暴露 `list_network_listeners`，后续只暴露监听与进程摘要；首轮返回合法或无效无工具响应时仍通过既有 Tool Runtime 执行一次监听工具并继续调查
- [x] 1.2 覆盖远端/目录来源不暴露或执行本机工具，以及工具失败、预算、未知工具和无证据结论继续安全收口
- [x] 1.3 覆盖后台观察启动稳定期：停止时不额外刷新，全新数据目录以前两次完整观察完成稳定基线，基线后的服务变化仍按两次确认规则触发
- [x] 1.4 覆盖 Windows psutil 与 macOS `lsof` 排除带远端地址的 UDP 客户端连接，同时保留未连接 UDP 监听

## 2. 最小实现

- [x] 2.1 调整后台观察循环和全新数据目录的双观察稳定基线，避免 Windows/macOS 面板或短命启动端口形成启动 incident
- [x] 2.2 收紧本机 `service_added` 的场景工具面并调整首轮无工具分支，复用既有 `list_network_listeners`、调用预算、审计和证据合同，并阻止远端来源借用当前节点工具
- [x] 2.3 在既有平台监听读取边界排除可识别的 UDP 客户端连接，不按端口或进程名过滤真实监听

## 3. 质量与真实验收

- [x] 3.1 运行受影响回归、完整测试、Ruff、Pyright 和 OpenSpec strict 校验
- [x] 3.2 从同一 CI revision 取得 Windows/macOS 正式包，各运行 5 个隔离 `service_added` 样本并记录工具选择正确率、证据结论完成率、错误参数/无关调用、安全拦截、延迟和模型请求上界
- [x] 3.3 确认 10 个样本至少 8 个取得相关只读工具证据、至少 7 个形成证据化结论，且无无证据确认、禁止工具执行或无关工具调用；清理全部临时进程、端口和数据
- [ ] 3.4 由独立只读任务审计规格、实现、测试和实机证据，并修复后复审至无阻断项
