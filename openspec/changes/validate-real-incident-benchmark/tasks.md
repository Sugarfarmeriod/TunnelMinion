## 1. 评测合同与固定证据

- [ ] 1.1 先补测试：捕获真实模式的模型请求，证明评分字段不泄露、模型请求与 Runtime fallback 分开计数、非预期或非法参数调用被如实记录
- [ ] 1.2 扩展 incident 场景与报告合同，新增场景相关工具结果、根因评分词、逐轮元数据、token、任务完成/参数/安全指标及代码版本，同时保持 v2 脚本化回归兼容
- [ ] 1.3 新增 v3 固定矩阵，以真实结构化工具证据覆盖既有类别和证据冲突，并验证正常刷新零 incident、零模型调用

## 2. 真实模型运行入口

- [ ] 2.1 复用 detector、IncidentInvestigator、ToolRuntime 和 SQLite 实现 Provider 注入与串行真实矩阵，模型失败场景使用确定性故障注入
- [ ] 2.2 新增 OpenAI-compatible 真实 incident CLI，记录 endpoint 之外的安全元数据、被测提交和机器可读报告，不读取凭据、不管理模型进程
- [ ] 2.3 用内存 Provider/HTTP mock 验证真实工具选择、冲突保持未知、token 可得与不可得、质量目标和安全硬门槛，确保一次失败不会被改写成满分

## 3. 验证、证据与审计

- [ ] 3.1 运行 incident 定向测试、兼容回归、类型/格式门禁和 OpenSpec strict validation，提交并推送实现恢复点
- [ ] 3.2 在不改变 8080 服务或真实网络的前提下，对当前 Qwen endpoint 串行运行一次完整 v3 矩阵，保存脱敏报告并核对模型、Prompt、工具、数据集、提交、延迟、token 和全部逐场景轨迹
- [ ] 3.3 由独立只读任务审计规格、实现、报告真实性和安全边界；主写任务修复有效问题并完成复审
- [ ] 3.4 运行最终质量门禁，完成 OpenSpec tasks 与归档，为单一阶段 Pull Request、CI 和 squash 合并准备可核验证据
