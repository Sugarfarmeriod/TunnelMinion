# TunnelMinion A/B 安全共享操作前基线

日期：2026-07-24

本文只记录仓库已有验收证据和本机只读连通检查，不包含 Gateway token、Authorization 头、WireGuard 私钥、完整进程正文或容器正文。

## 当前拓扑

| 项目 | Windows A | macOS B |
|---|---|---|
| WireGuard 地址 | `10.77.0.2` | `10.77.0.1` |
| 现有接口 | `HomeMac`，状态为 `Preferred` | `utun4`，只读工具因普通用户权限显示 `degraded` |
| 本地面板 | 默认 `127.0.0.1:8765` | 默认 `127.0.0.1:8765` |
| Tool Gateway | 固定 peer 客户端 | `10.77.0.1:8787` |
| 应用认证 | 与 WireGuard 密钥分离的 TunnelMinion token | 未认证请求返回 `401` |

2026-07-24 从 A 对 `10.77.0.1:8787` 的只读 TCP 检查成功，源地址为 `10.77.0.2`，接口为 `HomeMac`。该结果只证明现有 Gateway 可达，不授权或执行任何共享操作。

## 正式状态基线

- 既有最终验收记录显示 B 的 WireGuard 接口为 `utun4`，Docker 数据源为 `available`，正式容器计数为 3。
- 现有 Gateway 协议为 `1.0`，只暴露六个固定只读工具。
- 当前代码没有 L2/L3 写工具，不能创建监听、修改网络、控制 Docker 或重启服务。
- 正式 A/B 凭据和节点 ID 沿用现有本地配置；新技术验证不得读取、复制或记录完整秘密。

## 隔离技术验证资源

- 上游 fixture：`127.0.0.1:18880`。
- 临时代理入口：A 本机 WireGuard 地址 `10.77.0.2:18881`。
- 允许的验证端口范围：`18880`～`18899`。
- 租约标记：仅写入临时 `.data` 路径，不复用正式数据库。
- 技术验证结束后必须确认上述端口无监听、临时租约不存在。

## 恢复检查表

- [ ] `10.77.0.1:8787` 仍可达且未认证请求仍返回 `401`。
- [ ] `HomeMac` 地址和接口状态与验证前一致。
- [ ] `18880`～`18899` 没有遗留 TunnelMinion 验证监听。
- [ ] 临时租约和 fixture 数据已删除。
- [ ] 正式 Gateway、Docker、服务和用户 WireGuard 配置未被修改。

该基线将在真正启用 B 上的临时入口前重新采集；当前文件不能替代后续 A/B 状态不变验收。
