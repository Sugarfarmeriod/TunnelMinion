# TunnelMinion 项目协作约定

本文件适用于整个仓库。所有在本项目中工作的 Agent 和自动化工具都必须遵守以下约定。

## Git 分支命名

- 禁止使用 `codex/`、个人名称或具体 AI 工具名称作为分支前缀。
- 新功能默认使用 `feature/<简短主题>`。
- 缺陷修复使用 `fix/<简短主题>`。
- 文档修改使用 `docs/<简短主题>`。
- 重构使用 `refactor/<简短主题>`。
- 测试修改使用 `test/<简短主题>`。
- 持续集成修改使用 `ci/<简短主题>`。
- 构建与依赖修改使用 `build/<简短主题>`。
- 普通维护使用 `chore/<简短主题>`。
- 紧急生产修复使用 `hotfix/<简短主题>`；发布准备使用 `release/<版本或主题>`。
- `<简短主题>` 使用小写英文和连字符，例如 `feature/model-provider`、`fix/gateway-timeout`。
- 如果工作同时涉及多种类型，按主要交付目的选择前缀，不叠加多个前缀。
- 开始一个新的独立功能、缺陷修复、文档或基础设施阶段前，必须从合适且已同步的基线创建与工作
  类型匹配的新分支；不得为了省事在名称和范围已经不相符的旧功能分支上继续堆叠新主题。
- 同一 OpenSpec 变更内只有在工作仍属于同一个明确交付主题时才复用当前分支；进入可独立评审的
  新主题时创建新分支和 Pull Request。

## 提交与合并

- Git 提交遵循 Conventional Commits，例如 `feat: ...`、`fix: ...`、`docs: ...`。
- 功能开发通过 Pull Request 合并到 `main`，除非用户明确要求直接提交。
- 创建分支前先确认当前工作区状态，保留并避免覆盖用户已有修改。
- 每完成一个可独立验收的 OpenSpec 阶段或任务组，先运行与该阶段相称的质量门禁，再立即创建一次
  范围清晰的提交；禁止把多个已完成阶段长期堆积在未提交工作树中。
- 阶段提交成功后立即推送当前功能分支到 `origin`，为本地故障和误操作保留远端恢复点；除非用户
  明确要求暂不推送。
- 提交前使用 `git status` 与 diff 确认范围，运行时日志、临时数据、秘密和无关用户修改不得进入提交。

## 语言

- 说明性文档、代码注释和 docstring 使用中文。
- 标识符、命令、协议字段、文件格式保留字段和代码语法使用英文。
- OpenSpec schema 要求的固定标题与关键字保持原格式。

## 多任务与新任务协作

- 本节所称“新任务”是 Codex 中用户可见的新对话，也可称子线程；它会显示在侧边栏，拥有独立上下文，
  不是当前任务内部的 subagent。
- “新任务”或“子线程”的授权只允许创建上述用户可见的新对话，不允许用当前任务内部的 subagent
  代替。只有用户明确要求使用 subagent、子 Agent 或内部代理时才允许创建；两类协作方式归属不清时
  必须先确认。
- 只有工作能够独立推进、独立验证或独立评审时才创建新任务；不得为了增加并发而拆分强耦合步骤。
- 用户授权多任务协作后，协调者可在授权范围内自主创建、分配、等待和复核新任务，无需逐项再次确认；
  不得借此扩大原任务的权限、写入范围或外部操作范围。
- 新任务的提示必须写清基线提交、分支或 worktree、允许写入的范围、禁止触碰的文件和外部系统、验收标准，
  以及是否允许它继续创建新任务。模型或推理强度不会扩大任务权限。
- 新任务拥有独立 worktree 或分支时，必须从提示指定且已同步的基线开始，只在获准范围内写入，并遵守
  本文件的分支命名、质量门禁、阶段提交和及时推送要求。
- 每个阶段必须明确一条主写任务。相同文件和同一个外部架构图在同一阶段只能有一个写入者；公共配置、
  OpenSpec tasks 清单和集成分支始终只能有一个写入者，其他任务默认只读并把结论交给主写任务整合。
- 只读审计任务只允许执行 `git status`、`git show`、`git diff`、搜索、读取和不会改变项目或外部环境
  的验证命令；不得修改文件、勾选任务、提交、推送或写入外部系统。
- 独立实现、架构、安全和跨 change 冲突分析任务，在环境支持且用户未另行指定时，优先使用
  `gpt-5.6-luna` 的 `max` 推理强度；以阅读、清单核对或文档审计为主的只读任务可使用
  `gpt-5.6-sol` 的 `medium` 推理强度。指定模型不可用时必须明确报告，不得静默替换。
- 协调者必须核对各任务实际所在的提交、工作树状态、测试输出和证据，再决定是否更新 OpenSpec、提交、
  合并或进入下一阶段；不得把未经核验的任务结论、历史证据或降级路径成功当作本轮完成证据。
- 出现同文件写入、OpenSpec task 状态漂移、基线变化或跨分支职责重叠时，暂停相关写入任务，由一个
  明确的整合者先解决归属和合并顺序，再恢复并发；禁止使用 force-push、`git reset --hard` 或覆盖其他
  worktree 来处理并发冲突。
