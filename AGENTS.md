# TunnelMinion 项目协作约定

本文件适用于整个仓库。所有在本项目中工作的 Agent 和自动化工具都必须遵守以下约定。

## Git 分支命名

- 禁止使用 `codex/`、个人名称或具体 AI 工具名称作为分支前缀。
- 新功能默认使用 `feature/<简短主题>`。
- 缺陷修复使用 `fix/<简短主题>`。
- 文档修改使用 `docs/<简短主题>`。
- 重构使用 `refactor/<简短主题>`。
- 测试修改使用 `test/<简短主题>`。
- 持续集成修改使用 `ci/<简短主题>`。
- 构建与依赖修改使用 `build/<简短主题>`。
- 普通维护使用 `chore/<简短主题>`。
- 紧急生产修复使用 `hotfix/<简短主题>`；发布准备使用 `release/<版本或主题>`。
- `<简短主题>` 使用小写英文和连字符，例如 `feature/model-provider`、`fix/gateway-timeout`。
- 如果工作同时涉及多种类型，按主要交付目的选择前缀，不叠加多个前缀。
- 开始一个新的独立功能、缺陷修复、文档或基础设施阶段前，必须从合适且已同步的基线创建与工作
  类型匹配的新分支；不得为了省事在名称和范围已经不相符的旧功能分支上继续堆叠新主题。
- 同一 OpenSpec 变更内只有在工作仍属于同一个明确交付主题时才复用当前分支；进入可独立评审的
  新主题时创建新分支和 Pull Request。

## 提交与合并

- Git 提交遵循 Conventional Commits，例如 `feat: ...`、`fix: ...`、`docs: ...`。
- 功能开发通过 Pull Request 合并到 `main`，除非用户明确要求直接提交。
- 创建分支前先确认当前工作区状态，保留并避免覆盖用户已有修改。
- 每完成一个可独立验收的 OpenSpec 阶段或任务组，先运行与该阶段相称的质量门禁，再立即创建一次
  范围清晰的提交；禁止把多个已完成阶段长期堆积在未提交工作树中。
- 阶段提交成功后立即推送当前功能分支到 `origin`，为本地故障和误操作保留远端恢复点；除非用户
  明确要求暂不推送。
- 提交前使用 `git status` 与 diff 确认范围，运行时日志、临时数据、秘密和无关用户修改不得进入提交。

## 语言

- 说明性文档、代码注释和 docstring 使用中文。
- 标识符、命令、协议字段、文件格式保留字段和代码语法使用英文。
- OpenSpec schema 要求的固定标题与关键字保持原格式。

## 多对话任务与子 Agent 协作

- 本文件中的“子线程”“新线程”“新任务”或“新对话”均指 Codex 侧边栏中用户可见、可由用户独立
  继续沟通的新对话任务；不得把这些词解释为当前对话内部的子 Agent，也不得用子 Agent 代替用户要求
  创建的新对话任务。
- “子 Agent”或“内部代理”仅指当前对话内部、不会作为独立对话出现在用户侧边栏中的委派执行者。
  只有用户明确要求使用子 Agent、内部代理或等价表述时才允许创建；用户要求“子线程”或“新任务”
  本身不构成创建子 Agent 的授权。若用户同时提到两类协作方式但归属不清，必须先确认，不得自行替换。
- 只有工作能够独立推进、独立验证或独立评审时才创建用户可见的新对话任务；不得为了增加并发而拆分
  强耦合步骤。
- 每个阶段必须明确一条主写对话任务。相同文件、同一 OpenSpec task 清单和同一个外部架构图在同一阶段
  只能有一个写入者；其他对话任务默认只读，并把结论交给主写对话任务整合。
- 只读审计对话任务只允许执行 `git status`、`git show`、`git diff`、搜索、读取和不会改变项目或外部环境
  的验证命令；不得修改文件、勾选任务、提交、推送或写入外部系统。
- 创建用户可见的新对话任务时必须写清基线提交、分支或 worktree、允许写入的范围、禁止触碰的文件和
  外部系统、验收标准，以及是否允许继续创建新对话任务或委派子 Agent。模型或推理强度不会扩大任务
  权限。
- 独立实现、架构、安全和跨 change 冲突分析对话任务，在环境支持且用户未另行指定时，优先使用
  `gpt-5.6-luna` 的 `max` 推理强度；以阅读、清单核对或文档审计为主的只读对话任务可使用
  `gpt-5.6-sol` 的 `medium` 推理强度。指定模型不可用时必须明确报告，不得静默替换。
- 子 Agent 只用于边界清楚、结果可复核的子问题，并继承父对话任务的写入范围、禁止项和安全边界；
  父对话任务对其结论负责，不能直接把未经核验的口头结论当作完成证据。
- 协调对话任务负责核对各对话任务实际所在的提交、工作树状态、测试输出和证据，再决定是否更新
  OpenSpec、提交或进入下一阶段；历史证据和降级路径成功不能抹去本轮失败或阻塞。
- 出现同文件写入、OpenSpec task 状态漂移、基线变化或跨分支职责重叠时，暂停相关写入对话任务，由一个
  明确的整合者先解决归属和合并顺序，再恢复并发。
- 各写入对话任务仍须遵守本文件的分支、质量门禁、阶段提交和及时推送要求；禁止使用 force-push、
  `git reset --hard` 或覆盖其他 worktree 来处理并发冲突。
